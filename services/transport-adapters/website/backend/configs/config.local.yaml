decision_engine:
  url: http://localhost:8080
  timeout: 10s

server:
  address: ":8081"
  read_buffer_size: 1024
  write_buffer_size: 1024
  allowed_origins:
    - http://localhost:8081

log:
  level: info
  format: text
